<UserControl x:Class="QuacTestSuite.Views.HybridCryptoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Info Banner -->
        <Border Grid.Row="0" Background="#1A2D42" CornerRadius="12" Padding="20" Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <materialDesign:PackIcon Kind="ShieldLock" Width="40" Height="40" Foreground="#00D4AA" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Hybrid Post-Quantum Cryptography" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                    <TextBlock Text="Combine classical and post-quantum algorithms for defense-in-depth. If either algorithm remains secure, your data stays protected." 
                               Foreground="#7F8C9A" FontSize="13" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- KEM Hybrid -->
            <Border Grid.Column="0" Background="#152232" CornerRadius="12" Padding="24" Margin="0,0,12,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <materialDesign:PackIcon Kind="KeyLink" Width="28" Height="28" Foreground="#00D4AA" VerticalAlignment="Center"/>
                        <TextBlock Text="Hybrid Key Encapsulation" FontSize="18" FontWeight="SemiBold" 
                                   Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="CLASSICAL ALGORITHM" Foreground="#5A6C7D" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <ComboBox x:Name="CmbClassicalKem" SelectedIndex="1" Margin="0,0,0,20">
                        <ComboBoxItem Content="X25519 (Curve25519 ECDH)"/>
                        <ComboBoxItem Content="ECDH P-384"/>
                        <ComboBoxItem Content="ECDH P-521"/>
                        <ComboBoxItem Content="RSA-3072"/>
                    </ComboBox>

                    <TextBlock Text="POST-QUANTUM ALGORITHM" Foreground="#5A6C7D" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <ComboBox x:Name="CmbPqKem" SelectedIndex="1" Margin="0,0,0,20">
                        <ComboBoxItem Content="ML-KEM-512 (Kyber)"/>
                        <ComboBoxItem Content="ML-KEM-768 (Kyber)"/>
                        <ComboBoxItem Content="ML-KEM-1024 (Kyber)"/>
                    </ComboBox>

                    <TextBlock Text="KEY DERIVATION" Foreground="#5A6C7D" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <ComboBox x:Name="CmbKdf" SelectedIndex="0" Margin="0,0,0,24">
                        <ComboBoxItem Content="HKDF-SHA384 (Concatenate)"/>
                        <ComboBoxItem Content="HKDF-SHA512 (XOR then KDF)"/>
                        <ComboBoxItem Content="KMAC256"/>
                    </ComboBox>

                    <!-- Combined Key Info -->
                    <Border Background="#0A1628" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="Combined Public Key:" Foreground="#7F8C9A"/>
                                <TextBlock Text="1,232 bytes" Foreground="White" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <TextBlock Text="Combined Ciphertext:" Foreground="#7F8C9A"/>
                                <TextBlock Text="1,136 bytes" Foreground="White" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Grid.Row="2">
                                <TextBlock Text="Derived Secret:" Foreground="#7F8C9A"/>
                                <TextBlock Text="48 bytes (384 bits)" Foreground="#00D4AA" HorizontalAlignment="Right"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <Button x:Name="BtnTestKem" Click="TestKem_Click"
                            Background="#00D4AA" Foreground="#0A1628" FontWeight="SemiBold" 
                            Padding="20,12" Height="Auto" MinHeight="48" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="TestTube" Width="20" Height="20" VerticalAlignment="Center"/>
                            <TextBlock Text="Test Hybrid KEM" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Signature Hybrid -->
            <Border Grid.Column="1" Background="#152232" CornerRadius="12" Padding="24" Margin="12,0,0,0">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <materialDesign:PackIcon Kind="FileSign" Width="28" Height="28" Foreground="#FFB800" VerticalAlignment="Center"/>
                        <TextBlock Text="Hybrid Digital Signatures" FontSize="18" FontWeight="SemiBold" 
                                   Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <TextBlock Text="CLASSICAL ALGORITHM" Foreground="#5A6C7D" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <ComboBox x:Name="CmbClassicalSig" SelectedIndex="1" Margin="0,0,0,20">
                        <ComboBoxItem Content="Ed25519"/>
                        <ComboBoxItem Content="ECDSA P-384"/>
                        <ComboBoxItem Content="ECDSA P-521"/>
                        <ComboBoxItem Content="RSA-3072 PSS"/>
                    </ComboBox>

                    <TextBlock Text="POST-QUANTUM ALGORITHM" Foreground="#5A6C7D" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <ComboBox x:Name="CmbPqSig" SelectedIndex="1" Margin="0,0,0,20">
                        <ComboBoxItem Content="ML-DSA-44 (Dilithium-2)"/>
                        <ComboBoxItem Content="ML-DSA-65 (Dilithium-3)"/>
                        <ComboBoxItem Content="ML-DSA-87 (Dilithium-5)"/>
                        <ComboBoxItem Content="SLH-DSA-128f (SPHINCS+)"/>
                    </ComboBox>

                    <TextBlock Text="COMPOSITION METHOD" Foreground="#5A6C7D" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <ComboBox x:Name="CmbComposition" SelectedIndex="0" Margin="0,0,0,24">
                        <ComboBoxItem Content="Concatenation (sig1 || sig2)"/>
                        <ComboBoxItem Content="Nested (sign sig1 with alg2)"/>
                    </ComboBox>

                    <!-- Combined Signature Info -->
                    <Border Background="#0A1628" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="Combined Public Key:" Foreground="#7F8C9A"/>
                                <TextBlock Text="2,048 bytes" Foreground="White" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Grid.Row="1" Margin="0,0,0,8">
                                <TextBlock Text="Combined Signature:" Foreground="#7F8C9A"/>
                                <TextBlock Text="3,389 bytes" Foreground="White" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Grid.Row="2">
                                <TextBlock Text="Security Level:" Foreground="#7F8C9A"/>
                                <TextBlock Text="NIST Level 3 + 128-bit classical" Foreground="#FFB800" HorizontalAlignment="Right"/>
                            </Grid>
                        </Grid>
                    </Border>

                    <Button x:Name="BtnTestSig" Click="TestSig_Click"
                            Background="#FFB800" Foreground="#0A1628" FontWeight="SemiBold" 
                            Padding="20,12" Height="Auto" MinHeight="48" Cursor="Hand">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="TestTube" Width="20" Height="20" VerticalAlignment="Center"/>
                            <TextBlock Text="Test Hybrid Signature" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
