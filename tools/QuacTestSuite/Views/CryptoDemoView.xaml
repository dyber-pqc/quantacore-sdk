<UserControl x:Class="QuacTestSuite.Views.CryptoDemoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Algorithm Selection Bar -->
        <Border Grid.Row="0" Background="#152232" CornerRadius="12" Padding="20" Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Algorithm:" Foreground="White" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <ComboBox x:Name="CmbAlgorithm" SelectedIndex="1" Width="200" VerticalAlignment="Center">
                        <ComboBoxItem Content="ML-KEM-512"/>
                        <ComboBoxItem Content="ML-KEM-768"/>
                        <ComboBoxItem Content="ML-KEM-1024"/>
                    </ComboBox>
                    
                    <TextBlock Text="Key:" Foreground="White" VerticalAlignment="Center" Margin="24,0,12,0"/>
                    <ComboBox x:Name="CmbKey" SelectedIndex="0" Width="200" VerticalAlignment="Center">
                        <ComboBoxItem Content="production-key-2025"/>
                        <ComboBoxItem Content="test-key"/>
                        <ComboBoxItem Content="Generate New..."/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Border Background="#0A1628" CornerRadius="8" Padding="12,8" Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ShieldCheck" Width="16" Height="16" Foreground="#2ED573" VerticalAlignment="Center"/>
                            <TextBlock Text="NIST Level 3" Foreground="#2ED573" FontSize="12" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#0A1628" CornerRadius="8" Padding="12,8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Chip" Width="16" Height="16" Foreground="#00D4AA" VerticalAlignment="Center"/>
                            <TextBlock Text="Hardware Accelerated" Foreground="#00D4AA" FontSize="12" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Plaintext Panel -->
            <Border Grid.Column="0" Background="#152232" CornerRadius="12" Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FileDocument" Width="24" Height="24" Foreground="#00D4AA" VerticalAlignment="Center"/>
                            <TextBlock Text="Plaintext" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <Button Click="LoadFile_Click" HorizontalAlignment="Right" Background="Transparent" 
                                BorderBrush="#5A6C7D" BorderThickness="1" Foreground="#7F8C9A" 
                                Padding="12,6" Height="Auto" MinHeight="36" Cursor="Hand">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FolderOpen" Width="16" Height="16" VerticalAlignment="Center"/>
                                <TextBlock Text="Load File" Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Border Grid.Row="1" Background="#0A1628" CornerRadius="8" Padding="16">
                        <TextBox x:Name="TxtPlaintext" 
                                 Text="Enter your message here..."
                                 Background="Transparent" BorderThickness="0"
                                 Foreground="White" FontFamily="Consolas" FontSize="13"
                                 AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"/>
                    </Border>

                    <StackPanel Grid.Row="2" Margin="0,12,0,0">
                        <Grid>
                            <TextBlock Text="Size:" Foreground="#5A6C7D" FontSize="12"/>
                            <TextBlock x:Name="TxtPlaintextSize" Text="0 bytes" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Arrow / Action Buttons -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <Button x:Name="BtnEncrypt" Click="Encrypt_Click" Width="50" Height="50" 
                        Background="#00D4AA" Foreground="#0A1628" Padding="0" Cursor="Hand"
                        ToolTip="Encrypt" Margin="0,0,0,12">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="25">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <materialDesign:PackIcon Kind="ArrowRight" Width="24" Height="24"/>
                </Button>
                <Button x:Name="BtnDecrypt" Click="Decrypt_Click" Width="50" Height="50" 
                        Background="#00B4D8" Foreground="#0A1628" Padding="0" Cursor="Hand"
                        ToolTip="Decrypt">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="25">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <materialDesign:PackIcon Kind="ArrowLeft" Width="24" Height="24"/>
                </Button>
            </StackPanel>

            <!-- Ciphertext Panel -->
            <Border Grid.Column="2" Background="#152232" CornerRadius="12" Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Lock" Width="24" Height="24" Foreground="#FFB800" VerticalAlignment="Center"/>
                            <TextBlock Text="Ciphertext" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Click="CopyCiphertext_Click" Background="Transparent" 
                                    BorderBrush="#5A6C7D" BorderThickness="1" Foreground="#7F8C9A" 
                                    Padding="8,6" Height="Auto" MinHeight="36" Cursor="Hand" Margin="0,0,8,0">
                                <materialDesign:PackIcon Kind="ContentCopy" Width="16" Height="16"/>
                            </Button>
                            <Button Click="SaveCiphertext_Click" Background="Transparent" 
                                    BorderBrush="#5A6C7D" BorderThickness="1" Foreground="#7F8C9A" 
                                    Padding="12,6" Height="Auto" MinHeight="36" Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ContentSave" Width="16" Height="16" VerticalAlignment="Center"/>
                                    <TextBlock Text="Save" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="1" Background="#0A1628" CornerRadius="8" Padding="16">
                        <TextBox x:Name="TxtCiphertext" 
                                 Text=""
                                 Background="Transparent" BorderThickness="0"
                                 Foreground="#FFB800" FontFamily="Consolas" FontSize="13"
                                 AcceptsReturn="True" TextWrapping="Wrap" IsReadOnly="True"
                                 VerticalScrollBarVisibility="Auto"/>
                    </Border>

                    <StackPanel Grid.Row="2" Margin="0,12,0,0">
                        <Grid>
                            <TextBlock Text="Size:" Foreground="#5A6C7D" FontSize="12"/>
                            <TextBlock x:Name="TxtCiphertextSize" Text="0 bytes" Foreground="White" FontSize="12" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Stats Bar -->
        <Border Grid.Row="2" Background="#152232" CornerRadius="12" Padding="20" Margin="0,24,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="ENCAPSULATION TIME" Foreground="#5A6C7D" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TxtEncapTime" Text="--" FontSize="20" FontWeight="Bold" Foreground="#00D4AA" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="DECAPSULATION TIME" Foreground="#5A6C7D" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TxtDecapTime" Text="--" FontSize="20" FontWeight="Bold" Foreground="#00B4D8" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="SHARED SECRET" Foreground="#5A6C7D" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TxtSharedSecret" Text="32 bytes" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <TextBlock Text="EXPANSION RATIO" Foreground="#5A6C7D" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TxtExpansion" Text="--" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                    <TextBlock Text="STATUS" Foreground="#5A6C7D" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="TxtStatus" Text="Ready" FontSize="20" FontWeight="Bold" Foreground="#2ED573" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
